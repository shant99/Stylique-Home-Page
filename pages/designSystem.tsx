import type { NextPage } from 'next';
import styles from '../styles/designSystem.module.scss'
const DesignSystem: NextPage = ({products}: any) => {

    return (
      <div className={styles['design-system-container']}>
            Design 
      </div>
    )
  }

  export default DesignSystem